# SDD Tasks — shared-appearance-config

- [x] Add and install the shared `AppearanceConfig` value and JSON loader.
- [x] Add validation, scaled-token derivation, and style-precedence behavior.
- [x] Register `HnAppearance` with live file/directory watching and public QML properties.
- [x] Route `HnSurfaceFrame` resolution through `HnAppearance` without breaking `HnShapeProfile` compatibility.
- [x] Export the singleton through canonical and lowercase modules.
- [x] Add focused C++ and QML regression tests.
- [x] Build, format, and run focused and full verification.
