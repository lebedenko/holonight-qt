# UQC-219 — Batch 3 lifecycle and historical crash diagnosis

State: Done for the bounded diagnosis handoff (2026-09-16). Ready recorded before local assignment at umbrella `12ef2c8`.
Baseline: `0e0f92efe1517bc07c577299f07b51117f25f05a`, clean and confirmed on
canonical origin/main with `git ls-remote` before assignment.

## Scope and contract

Extend existing SharedRendering teardown coverage with the implicated installed
Kirigami/FormCard application composition. Correlate retained ScrollBar warnings
with lifecycle records first. Change provider bindings only after a failing
baseline regression. Preserve public APIs, runtime style selection and accepted
rendering. Consumer and third-party sources remain unchanged.

Reuse the retained Haruna SIGABRT core and matching provider build identity;
inspect thread/lifetime evidence and exercise only supported hypotheses through
focused reproducers. Successful retries cannot resolve the historical crash.
Umbrella owns original evidence and application dispositions.

Fusion is research-gated by the umbrella. Inconclusive research means
research-pending, with no provider hover instrumentation or manual kit.
Exclude F05, Tokodon chevron, closed G07, layouts and final integration.

## Verification and handoff

Run narrow lifecycle/reproducer checks in both styles at DPR 1 and 1.25, then
installed-consumer acceptance, full provider tests and applicable static checks
for provider changes. Publish verified work before the umbrella pin changes.
A bounded diagnostic handoff may complete UQC-219 while UQC-207 gates stay open.
Generate an immutable kit only if missing evidence or repair acceptance needs one.

## Implemented coverage and disposition

`tests/test_shared_rendering.cpp` adds two focused cases to the existing
SharedRendering matrix, with no production QML changes:

- Installed FormCardPage containing FormCard, FormComboBoxDelegate and overflowing
  content, with repeated creation/destruction and garbage collection. The installed
  optional dependency follows the existing availability/skip convention; it was
  present and the test ran in this verification.
- Asynchronous secondary ApplicationWindow with reusable ItemDelegates, navigation,
  completed loads, immediate cancellation and recreation. Assert descendants retain
  GUI-thread affinity; measure actual DPR and synchronization thread. Broad logging
  in a private headless Wayland compositor exercises the supported historical
  thread-boundary hypothesis without desktop input automation.

Neither case fails on the assigned provider baseline. Existing popup, ScrollView,
window and application-engine teardown checks also pass. No speculative ScrollBar
binding guard or ItemDelegate change is justified. The umbrella records stronger
historical core evidence (rectangle belongs to the settings render thread) and
explicitly retains the unresolved initiating trigger and original SIGABRT.

Current verification uses Qt declarative 6.11.2-2, Kirigami 6.30.0-1 and addons
1.13.1-1. Historical core analysis separately uses build-ID-matched Qt declarative
6.11.2-1 binaries. Fresh passing runs are not a replay of the original Intel/Mesa
application session and do not close its crash or actual-app ScrollBar warnings.
Fusion research is inconclusive for both Settings and Haruna; no local hover work.

## Verification — 2026-09-16

Evidence logs are retained in umbrella `.cache/uqc219/`.

- `cmake --build holonight-qt/build --target holonight_runtime_composite_tests -j 4`:
  pass, then final full build passes.
- `--gtest_filter=SharedRendering.*Teardown:SharedRendering.AsyncSettingsDelegatesKeepGuiThreadAffinity`:
  six tests pass in each style/DPR process (24 checks). FormCard broad-debug
  software comparison also passes.
- `ctest --test-dir holonight-qt/build -R 'holonight_shared_rendering|holonight_package_install_test' --output-on-failure`:
  5/5 pass, including installed-consumer acceptance.
- Final `ctest --test-dir holonight-qt/build --output-on-failure -j 4`: **87/87 pass**,
  including all four updated shared-rendering cases and installed acceptance.
- Isolated `cage` with `WLR_BACKENDS=headless`, `WLR_RENDERER=pixman`, private
  XDG_RUNTIME_DIR, `QT_QPA_PLATFORM=wayland`, `QT_QUICK_BACKEND=rhi`,
  `QSG_RHI_BACKEND=opengl`, `QSG_RENDER_LOOP=threaded`, `LIBGL_ALWAYS_SOFTWARE=1`,
  `QT_LOGGING_RULES=*.debug=true`, explicit styles and scales: **8/8 tests pass**.
  Final logs report actual DPR 1/1.25 and `threadedSync=1` in both styles. Initial
  sandbox compositor attempts could not bind a socket; approved isolated reruns
  supply the usable results.
- clang-format and whitespace pass. Focused clang-tidy exits 0 with nonfatal
  test-style warnings (including complexity, braces and endl); no warning-free
  claim. Its initial invocation rejected GCC's `-mno-direct-extern-access`; a
  temporary compile database removes only that incompatible option.
- REUSE **395/395** passes after the sandbox blocked its multiprocessing socket.
  No QML source changes require a new QML lint run.

No new immutable kit or human comparison is required for this diagnostic handoff.
UQC-207 remains In Progress; umbrella UQC-201 remains In Progress and the initiative
Accepted. Publication is required before the umbrella pin is updated.
