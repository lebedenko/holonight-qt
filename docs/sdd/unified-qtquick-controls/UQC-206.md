# UQC-206 — Dropdown interaction investigation

State: In Progress. Scope made Ready after baseline reproductions on 2026-09-12,
then assigned for provider-only implementation. Repository: holonight-qt.
Baseline: `65c806fb6a65cae9652dde7a69d595813973c1a9`, clean main and canonical
origin/main confirmed with `git ls-remote` on 2026-09-12.

## Scope and contracts

Investigate D01 row disappearance, D02 popup height binding loops, D03 outside
dismissal and D04 current-row visibility/highlight/keyboard origin separately.
The umbrella findings register retains manual observations and acceptance gates.
No common root cause is assumed. Broad manual acceptance remains paused.

Settings Appearance uses HnIconComboBox with FontListModel (QAbstractListModel,
display role, reset when retained font changes). Greeter FooterSelector derives
from HnIconComboBox, binds delegateHeight to control height and uses session name
and layout label roles. Standard ComboBox and HnIconComboBox both own popup/list
implementations; their shared geometry helper must remain Controls-independent.

Use isolated offscreen windows with standard runtime ComboBox and HnIconComboBox,
short/long role models, upper/lower placement, hover/scroll/reopen and first/last
selection. Measure delegate text, lifetime, visibility and geometry, popup bounds,
outside dismissal, current highlight and keyboard origin. Treat QML binding-loop
warnings as failures. Compare HoloNight/Fusion at Qt scale 1/1.25. Preserve existing
transform/available-space geometry and installed-consumer tests. No live desktop
pointer/focus automation, consumer changes or new public API is authorized here.

Record reproduction evidence before transitioning Ready → In Progress and editing
product QML. After local repair verification and canonical publication, prepare a
short Settings/greeter dropdown manual checkpoint; do not repeat focus or Polkit
cancellation acceptance. D01–D04 stay open until supported by evidence.

## Reboot recovery

Temporary kits were wiped. Retained workspace caches and source remain available.
Reconfigure the existing config/provider builds against `.cache/uqc206/prefix`
under the umbrella, retaining new reproduction logs there so another reboot does
not erase investigation evidence. This is development setup, not a released kit.

## Baseline reproduction — 2026-09-12

The six-case offscreen interaction fixture reproduces D03 in both controls under
HoloNight and in the icon composite under Fusion. Effective closePolicy is
CloseOnPressOutsideParent | CloseOnEscape, while parent is the window overlay.
D01 reproduces in HnIconComboBox under HoloNight: currentItem belongs to a second,
hidden ListView consuming the same delegateModel. Its scene geometry is outside
the displayed popup and it is not visible/selectable. Standard HoloNight and
Fusion rows pass after rendering settles and the pointer is moved out before
reopening; initial standard-row failure was stale hover position in the fixture.
D04 keyboard reopening passes; D02 height warnings remain under investigation.
Logs: umbrella `.cache/uqc206/baseline-settled.log`, `parent-probe-2.log`,
`baseline-Fusion-1.25.log`. Product sources were unchanged during these probes.

Ready repair contract: only the popup currently assigned to the control consumes
its delegateModel; the replaced popup must not own its delegates. Overlay-parented
popups close on Escape or press outside the popup, preserving selection behavior.
Retain existing public APIs, transform geometry, limits and application overrides.

## Verified repair checkpoint — 2026-09-12

Both popup lists now consume delegateModel only when their popup is assigned to
root.popup. Replaced inherited popups therefore cannot steal active delegates.
Both overlay-parented popups explicitly close on Escape/outside press and are
modal without dimming. Modal input consumption is necessary: the initial
nonmodal fix closed on press then reopened on the collapsed control's release.
No geometry helper, public API or consumer source changes.

Ten parameterized regressions exercise runtime standard/icon controls: outside
click without activation, collapsed-area closure, current-row visibility/highlight
and keyboard origin on reopen, hovered first/last selection, and QAbstractListModel
font-role resets with short/long models at both window edges. Events target only
offscreen windows. Fusion can cover the collapsed control with popup rows, so that
case permits selecting the row under the click while requiring popup closure.
Tests wait for a rendered frame before measuring scene geometry and move the
pointer outside before reopening to avoid stale hover changing the current row.

Commands from the umbrella (logs retained in `.cache/uqc206/`):

```sh
cmake --build holonight-qt/build -j 4
ctest --test-dir holonight-qt/build -R holonight_dropdown_interaction --output-on-failure
ctest --test-dir holonight-qt/build --output-on-failure -j 4
clang-format --dry-run --Werror holonight-qt/tests/test_dropdown_interaction.cpp
git -C holonight-qt diff --check
git diff --check
```

- Focused final: 10/10 cases in each HoloNight/Fusion × scale 1/1.25 process.
- Full provider: 69/69 CTests pass, 42.30 seconds; installed-consumer check passes
  in 36.19 seconds, including the new interaction cases under both styles.
- Config/provider dependency prefix was recovered in persistent `.cache/uqc206`;
  the missing temporary patchelf was replaced in the local CMake cache with an
  existing executable from the persistent uv cache. No dependency downloads.
- Strict qmllint reports the existing `root.popup.background.semanticRadius`
  missing-property warning in ComboBox.qml; identical warning reproduced against
  baseline source. No warning in the icon composite. This is not a clean lint pass.
- Focused clang-tidy from the provider exits 0 with test-style warnings (protected
  fixture members, GTest macro cognitive complexity, short identifier/braces).
  Initial umbrella-directory invocation enabled no checks; it is not validation.
  Logs `tidy.log`, `tidy-provider.log` retain both results.

D01/D03 have automated repair evidence but are not manually closed. D02's reported
implicitHeight loop has not reproduced in these model/geometry checks; no height
formula repair is claimed. D04's isolated visibility/highlight/keyboard checks pass
on baseline and repaired code; original app-specific interpretation remains open.
UQC-206 stays In Progress pending those investigations and a focused manual kit.
No publication, gitlink update, manual acceptance or ecosystem integration is
implied by this uncommitted checkpoint. Existing umbrella documentation edits are
preserved. No temporary-kit recreation is needed for the already accepted focus
and Polkit cancellation checks.


Final baseline cross-check uses a private copy of built QML modules with the two
popup files replaced by exact HEAD sources. Removing only those modules' qmldir
resource preference forces the source fixtures instead of the repaired embedded
resources; QML disk cache is disabled. Ten final cases: baseline HoloNight fails
four at each scale (both outside-dismissal cases, icon row selection and icon
font-reset visibility); baseline Fusion fails only icon outside dismissal at each
scale. No D02 warning or D04 failure appears. Logs `baseline-source-*.log` retain
results. Earlier `baseline-final-*.log` accidentally selected embedded repaired
resources and are not baseline evidence. This cross-check does not modify product
sources or supersede original baseline-process reproductions.


## D04 clarified reproduction — 2026-09-12

The user reports that reopening starts the list at the beginning; the selected row
may be onscreen or offscreen and has no highlight. No keyboard-origin failure was
asserted. The earlier tests' passing highlight flags and list-local coordinates did
not prove that their selected delegate belonged to the displayed popup.

Added mouse-reopen coverage and actual selected-item visibility/scene containment
to both reopen tests. Baseline source modules now fail the two icon-composite
reopen cases under HoloNight at 1.25; standard ComboBox passes. The selected delegate
is in the hidden inherited list. Logs: `.cache/uqc206/d04-baseline.log` in umbrella.
This reproduces the missing visible highlight, not the exact scroll-reset sequence.
The existing D01 ownership repair addresses that reproduction without more product
QML edits. D04 remains open for the full reported behavior and manual acceptance.


Repaired verification: `ctest --test-dir holonight-qt/build -R
'holonight_dropdown_interaction|holonight_runtime_composites|holonight_package_install_test'
--output-on-failure` passes 7/7 in 43.52 seconds. All 12 dropdown cases pass in each
HoloNight/Fusion × scale 1/1.25 process; composite/geometry and installed-consumer
coverage pass. Log `.cache/uqc206/d04-verification.log`. Formatting and both repository
whitespace checks pass. Only test/SDD/coordination records changed in this follow-up.


## User-requested candidate verification kit — 2026-09-12

The user explicitly requested test scripts. Umbrella prepared a clearly unpublished
candidate kit at `/tmp/holonight-uqc206-wou7oqyf` with exact baseline revisions,
current product-QML patch, source parity and checksums. This is verification of the
working tree, not a published handoff or integration pin. See umbrella DROPDOWNS.md
for launch instructions, persistent archive, isolation and preparation evidence.
Installed 12-case regressions pass under both styles at scales 1/1.25; Settings and
greeter each pass four installed startup modes. Manual D01–D04 observations remain
pending. No further provider implementation changes for kit preparation.


## Manual checkpoint — 2026-09-12

The user reports successful mouse/keyboard dropdown behavior in Settings and
greeter demo under default/Fusion at requested scale 1.25. Umbrella FINDINGS.md owns
the four-run inventory and read-back. D01/D03/D04 are accepted for these owned
application checks, not the full third-party/ecosystem matrix. No repeat requested.

D02 is still reproduced in the real application: Settings default log has six
implicitHeight binding-loop warnings at the provider ComboBox popup. Other three
runs have no matches in the bounded diagnostic search. Next provider investigation
must reproduce that actual Settings geometry/model context; do not declare UQC-206
Done based on interactive success. Manual maps also show host config library and
missing LD_LIBRARY_PATH despite staged QML; retain the kit loading limitation.

New F06 stationary-pointer competing hover/highlight is a separate provider visual
investigation. F07 launcher hover changing the Enter target belongs to shell; no
shell implementation in this provider package. Proposed policy and distinct
acceptance checks are recorded once in the umbrella findings register. No new
product changes or tests in this evidence-recording turn.

## D02 reproduced and repaired — 2026-09-12

Full manual-log context corrects the earlier font attribution: the six warnings
occur while constructing Weather page ComboBoxes (refresh interval, pressure,
wind, temperature, location source and provider). No console warning was reported
by the user. The kit enables `*.debug=true`; `qt.quick.viewport.debug=true` alone
reproduces the loop in a reduced Settings-row Loader fixture. Ordinary logging
does not reproduce it. The font-model lifecycle remains compatibility coverage.

The dependency is popup implicitHeight → naturalHeight → list implicitHeight →
contentHeight → viewport diagnostics/geometry → popup implicitHeight. The owned
rows already have uniform, configurable delegateHeight. Their bounded content
demand now uses item count × delegateHeight, independent of the viewport. Actual
contentHeight still determines scrolling and clipping. No timers, fixed popup
height, warning suppression or geometry-helper changes. D01/D03/D04 edits retained.

Regression evidence under umbrella `.cache/uqc206`: `d02-weather-debug-baseline.log`
fails the current pre-repair candidate; `d02-weather-fixed.log` passes font and
Weather construction/opening after repair. Separate CTest style/scale processes
and installed-composite coverage enable the triggering viewport diagnostic.
`d02-focused.log`: 7/7 pass (44.62 s), including geometry, selected-row visibility,
font resets at both edges, both styles/scales and installed package coverage.
Canonical baseline `65c806fb6a65cae9652dde7a69d595813973c1a9` reconfirmed via
`git ls-remote origin refs/heads/main`. Initial sandbox SSH failure retained in
session output; permitted query succeeds. Full suite and manual Settings Weather
confirmation remain required before D02 closure.

Full provider suite: 69/69 pass (42.25 s), log `d02-suite.log`.
