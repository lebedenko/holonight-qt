# UQC-207 — shared rendering

State: In Progress (2026-09-14); implementation baseline `68b7069cc10b85cf0ce591b89cf8c1494bf316b3`.
Canonical origin/main and clean checkout confirmed from umbrella
`5822417dca1b8735bf4afbdb49ac7604ce283dca` before assignment.

## Confirmed scope and reproductions

Haruna 1.8.1 SettingsWindow uses ItemDelegate with an Action carrying `icon.name`
and explicitly binds `highlighted` to its navigation current index. Its ShortcutsSettings
uses a reusable ListView ItemDelegate with a replaced RowLayout content item,
IconTitleSubtitle and KeySequenceItem; it does not bind highlighted or checked.
The provider currently drops navigation icons entirely, MenuItem only consumes
icon.source, and ItemDelegate renders ListView.isCurrentItem as selection.
MenuItem also reserves an icon column even in menus containing no icons.

Reproduce these independently with runtime-selected standard controls: Action named
icons, URL icons, a menu with changing visible items, and an unselected ListView
delegate with replaced content. Establish failing baseline assertions before repair.
Retain explicit navigation highlighting and keyboard focus. Tests must use an
isolated icon fixture rather than depending on the host's icon theme.

## Repair design

- Use existing HnIcon rendering for ItemDelegate and MenuItem, accepting theme names
  and URLs. Preserve ItemDelegate content replacement and existing public APIs.
- Menu owns a reactive scan of its items. Reserve the shared icon column only for
  visible icon-bearing entries, updating on insertion/removal/visibility/icon changes.
  Retain check column, submenu arrow and mirrored leading/trailing padding.
- ItemDelegate selected overlay follows explicit highlighted/checked only; ListView
  current-index bookkeeping alone does not imply application selection. Keep hover,
  press and visualFocus independently represented.

- User clarified R04 on 2026-09-14: the focus ring must surround the whole Switch,
  not its thumb. The pixel-difference baseline measures only 16 logical pixels
  of feedback on a 120 × 36 control (19–20 physical pixels at DPR 1.25).
  Move the focus outline to a transparent full-control background using existing
  semantic radius and focus-border tokens; preserve track/thumb geometry and input.
- F04 plain Button Tab → Space → Tab → Backtab retains owner, reason and visualFocus
  in both styles/scales. Test connection is now identified as an example; its real
  asynchronous enabled-state lifecycle still needs attribution. Do not claim repair.

No dependencies, third-party patches, runtime selector changes or palette policy changes.

## Investigative boundaries

The original Haruna core/log survives. The fatal diagnostic is a cross-thread QML
property connection during background creation. A provisional retained-build symbol
lookup points at ItemDelegate anchors.fill; exact binary identity and root cause
remain to be established. Do not claim the selection repair fixes this crash.
Umbrella owns C06 crash/diagnostic classification and the repeated launch evidence.
F03/F04/R04 require actual AI control/owner evidence before extending this scope.
Compatibility findings C01/C02/C04/C05 remain umbrella-owned investigations.

## Acceptance

- [x] Failing baseline regressions for icons, conditional menu spacing and selection.
- [x] Repaired tests pass under HoloNight/Fusion at scales 1 and 1.25.
- [x] Insertion/removal, icon/visibility transitions, mirroring, explicit selection,
      checked/hover/focus and keyboard activation remain correct.
- [x] Focused provider, installed consumer, full provider and static checks pass.
- [ ] Publish verified provider handoff before umbrella pin; manual Batch 3 acceptance
      is recorded only in the umbrella FINDINGS.md.


## Verified rendering handoff — 2026-09-14

Changed Menu/MenuItem/ItemDelegate/Switch plus shared-rendering regressions,
CTest/installed-fixture registration and the existing ItemDelegate text-color smoke
assertion (the label is now inside an icon/text layout). No consumer source changed.

- Retained-baseline comparison: umbrella `.cache/uqc207/baseline-final.log`.
  HoloNight fails all three icon/menu/selection tests at both scales; Fusion passes
  applicable comparisons. Earlier fixture defects and failed runs remain retained.
- Switch baseline: `.cache/uqc207/focus-baseline3.log`. Whole-control outline assertions
  fail in both states/scales; plain button and submenu activation comparisons pass.
- Final focused 5/5 CTests (new matrix plus smoke), installed package acceptance,
  and full provider 77/77 CTests pass. Full log: `.cache/uqc207/full-provider-final.log`.
- clang-format check, provider/demo/gallery import policy and REUSE lint pass.
  QML lint succeeds with existing Popup/ComboBox diagnostics and no new warnings
  from changed files. Log: `.cache/uqc207/qmllint-final.log`.
- HoloNight-specific menu-column and Switch-outline assertions intentionally skip
  under Fusion. Icons, selection, menu activation and Button focus still run there.

The first full run caught an outdated smoke assertion reading color on the previous
Text contentItem. The corrected assertion reads the actual label and preserves the
expected color. No rendering expectation was weakened.

R01/R02/R03/R04 await actual-app manual acceptance. F03/F04 application attribution
and C06 crash-cause investigation remain open in the umbrella; passing synthetic
fixtures do not close them. Broader integration is not part of this handoff.
