# UQC-213 — Window input interaction (F06)

State: Ready, then assigned provider-only / In Progress on 2026-09-12.
Repository: holonight-qt. Canonical upstream baseline:
`65c806fb6a65cae9652dde7a69d595813973c1a9`, verified via origin/main ls-remote
2026-09-12. Preserve the accepted UQC-206 working-tree repair as a separate commit.
F06 is separate from UQC-207's unrelated rendering findings.

## Reproduction

`InputInteraction.KeyboardClearsStaleButtonHoverFeedback` fails before repair:
an offscreen HoloNight button retains hover fill after Right, without pointer
movement. Evidence: umbrella `.cache/uqc206/f06-baseline.log`. Fusion's standard
rendering is excluded by design; HoloNight-owned composites remain covered there.

## Shared contract (settled before implementation)

Holonight.Core exports style-independent attached `HnInputInteraction` on Items
and Windows. `hoverAllowed` is read-only; `suppressHover()` gives keyboard authority
to newly opened surfaces; `pointerMoved(point scenePosition)` reports changed
screen coordinates in the attached window's scene coordinates. Shared state is
owned per QQuickWindow. Non-modifier key presses suppress hover. Real MouseMove
with changed screen coordinates restores hover and emits movement even within an
already-hovered item. Enter/hover events, layout/animation/scroll, pointer presses
and wheel alone do not authorize hover selection. Input is observed, never consumed.
Clicks retain immediate activation. Different windows are independent; detached
items do not authorize hover, reattachment follows their new window safely.

Apply gating only to HoloNight-owned hover rendering in standard style controls
and composites, including selectable/icon delegates. Preserve selected, checked,
pressed and keyboard-focus rendering. Do not modify Fusion standard-control QML.

## Verification and handoff

Offscreen tests cover window independence, attachment/destruction, modifier keys,
stationary enter/reentry, scrolling/animation, keyboard suppression, deliberate
movement and immediate clicks, with representative button/delegate rendering.
Run both styles/scales, dropdown regressions, full provider suite, installed/Core
isolation and QML policy checks. Publish and verify canonical provider availability
before shell UQC-214 implementation/pinning. Manual F06 confirmation remains open.

## Local verification — 2026-09-12

Window-owned QObject observes key/mouse/wheel events without consumption. The
Core attached type tracks windowChanged and shares one observer per window;
window destruction detaches surviving consumers. Screen coordinates, rather than
local hover transitions, distinguish real movement. All owned hover paint/tooltip
references are gated, including composite delegates under Fusion. Icon delegates
explicitly enable hover because offscreen Fusion's native default disables it.

Five observer/rendering regressions per style/scale exercise stationary movement,
enter/reentry, wheel, animation, immediate click, keyboard/modifier use, checked
selection, independent windows and attachment/window destruction. Dropdown
coverage additionally protects keyboard highlight and Enter target on stationary
reopen. Native Fusion standard-control paint checks are explicitly skipped.

Full provider `ctest --test-dir holonight-qt/build --output-on-failure -j 4`:
73/73 pass, 42.73 s (`f06-suite-final.log`). Final extended animation/lifetime
coverage plus dropdown/composite/installed checks: 11/11, 37.39 s
(`f06-handoff.log`). Logs live in umbrella `.cache/uqc206`.
Core/style/composite qmllint targets exit 0, with existing Popup, ComboBox,
HnTextArea, HnLoadingState, HnKeyHint and HnColorPicker warnings retained; no new
attached-type diagnostics. Formatting and whitespace checks pass. Initial tidy
invocation rejects GCC's `-mno-direct-extern-access`; a private compilation-database
copy removes only that unsupported flag for the compatible rerun. Event casts
are guarded by QEvent types; moc-generated signal parameter-name warnings are
not product failures. The UQC-206 sizing commit is `975bf09`.

Repository-local automated work is complete pending publication confirmation.
F06 manual acceptance and D02's actual Settings Weather log remain open. This is
not ecosystem integration or a claim of new manual observations.

## Stationary-pointer selection follow-up — 2026-09-13

Reopened provider-only scope, Ready then In Progress after reproduction. Exact
clean baseline `4dfa8030df59b896ee7d8ef6a8d6ff6ab960c040` revalidated against
canonical origin/main with `git ls-remote` (sandbox SSH configuration failure,
permitted query succeeds). Existing umbrella manual failure records are preserved.

`KeyboardScrollDoesNotSelectRowUnderStationaryPointer` reproduces selection theft
after keyboard navigation scrolls the popup beneath an unmoved pointer. At the
eighth Down the highlight jumps to row 1 instead of row 8; Enter eventually commits
row 6 instead of row 20. Both owned dropdowns fail in HoloNight, and the icon
composite fails in Fusion. Logs: umbrella `.cache/uqc206/stationary-baseline*.log`.
The previous regression checked hover paint without waiting for scrolling frames.

Repair contract: owned delegates disable native hover handling while the existing
window input policy grants keyboard authority; opening an owned popup suppresses
stale hover. Deliberate movement restores hover handling, and direct clicks retain
activation. Preserve Qt's native keyboard/commit handling and public composite APIs.
Fusion standard controls retain their own policy. Add settled-frame navigation,
reopen and pointer recovery coverage before accepting the repair. No live desktop
pointer/focus automation or manual closure is implied.

Verification: final focused input/dropdown matrix passes 8/8 CTest entries (5.24 s),
including 22 dropdown cases per process (Fusion standard-owned policy cases
explicitly skip). Full provider suite passes 73/73 (43.64 s), including relocated
installed-consumer coverage (37.51 s). Tests wait for rendered scrolling/reopen,
assert the Enter target, and check deliberate movement plus immediate clicking
while keyboard hover suppression is active. Source formatting and whitespace pass.
Core policy/API and consumer source are unchanged. Style/composite qmllint targets
exit 0 with the existing Popup, ComboBox semanticRadius, HnTextArea, HnLoadingState,
HnKeyHint and HnColorPicker warnings; no new diagnostics. Logs are retained in
umbrella `.cache/uqc206/stationary-{recovery-focused,suite,qmllint}.log`.
Repository repair is locally verified; F06 manual acceptance remains open.
